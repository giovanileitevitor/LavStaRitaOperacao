<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LavStaRitaOperacao" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
    </style>

    <style name="Theme.LavStaRitaOperacao" parent="Base.Theme.LavStaRitaOperacao">
        <item name="colorPrimary">@color/blue_lav</item>
        <item name="colorPrimaryDark">@color/blue_lav</item>
        <item name="colorAccent">@color/blue_lav</item>
        <item name="android:statusBarColor">@color/blue_lav</item>
    </style>

</resources>